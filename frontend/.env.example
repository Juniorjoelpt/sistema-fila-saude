# URL base da API do backend
VITE_API_URL=http://localhost:8080

# Tenant (prefeitura) usado em desenvolvimento local. Em produção, o tenant
# é resolvido automaticamente pelo subdomínio (ex.: portopi.filasaude.com.br).
VITE_TENANT_ID=demo
